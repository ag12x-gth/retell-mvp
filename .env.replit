# ============================================================
# RETELL MVP - Configuração Replit
# ============================================================
# IMPORTANTE: Configure as variáveis de ambiente no Replit Secrets
# https://replit.com/@yourusername/your-repl-name → Secrets (🔒 no menu lateral)
# ============================================================

# Database (SQLite - já configurado)
DATABASE_URL="file:./prisma/dev.db"
NODE_ENV="production"
PORT=3000

# Twilio (Configure no Replit Secrets)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number
TWILIO_API_KEY=your_twilio_api_key
TWILIO_API_SECRET=your_twilio_api_secret

# Retell.ai (Configure no Replit Secrets)
RETELL_API_KEY=your_retell_api_key
RETELL_WORKSPACE_ID=your_retell_workspace_id

# OpenAI (Configure no Replit Secrets)
OPENAI_API_KEY=your_openai_api_key

# Webhook URLs (Será preenchido automaticamente pelo Replit)
WEBHOOK_BASE_URL=https://your-repl-name.yourusername.repl.co

# JWT Secret (Gere um aleatório)
JWT_SECRET=your_jwt_secret_here_change_this

# ============================================================
# INSTRUÇÕES:
# ============================================================
# 1. Não edite este arquivo diretamente no Replit
# 2. Use Replit Secrets (🔒) para adicionar as credenciais:
#    - TWILIO_ACCOUNT_SID
#    - TWILIO_AUTH_TOKEN
#    - TWILIO_PHONE_NUMBER
#    - TWILIO_API_KEY
#    - TWILIO_API_SECRET
#    - RETELL_API_KEY
#    - RETELL_WORKSPACE_ID
#    - OPENAI_API_KEY
#    - JWT_SECRET
# 
# 3. O Replit injetará automaticamente os secrets como variáveis de ambiente
# ============================================================
